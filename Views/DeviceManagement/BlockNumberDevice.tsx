import AddNewBlocking from "./AddNewBlocking";

const BlockNumberDevice = () => {
  return (
    <div>
      <AddNewBlocking title="Lock Account / Device " option={true} />
    </div>
  );
};

export default BlockNumberDevice;
